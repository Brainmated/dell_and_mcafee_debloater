Simply extract the directory anywhere on C:.

Then change to /removeundesiredapps/ directory and run on a terminal (doesn't matter if elevated or not):

```powershell.exe -ExecutionPolicy Bypass -File ".\remove-undesiredapps.ps1" -Verbose -RunMCPR```
